# Session 06 — Pre-work email (T-2 days)

**Subject:** EA × Insight Copilot Workshop — Session 06 prep (~10 min)

---

Session 06 (Spec-Kit Mastery Pt 1) runs **<DATE> at <TIME>**.

## Required

- **No install needed.** The spec-kit flow (`/specify`, `/plan`, `/tasks`, `/implement`) uses structured Copilot Chat prompt patterns — not an npm package. `@github/spec-kit` is not a published CLI.
- Confirm `output/ea-cpp-games/specs/lockless-ring-buffer/` contains only `README.md` (skeleton state). If you've run the spec-kit prompts there before, `git restore` it.
- Re-read articles 1–5 and 7 of `output/ea-cpp-games/specs/constitution.md`.

## Bring

A feature you'd like to drive through spec-kit live in office hours. We'll pick one for a follow-up demo.

— Insight delivery team
